from setuptools import setup, find_packages

setup(
    name="Level2VBackTest",
    version="0.1.0",
    packages=find_packages(),
    python_requires=">=3.8",
    # If you have required packages, list them here or in requirements.txt
    install_requires=[
        # "pandas",
        # "numpy",
        # "matplotlib",
        # etc.
    ],
)
